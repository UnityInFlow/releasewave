FROM alpine:3.20

RUN apk add --no-cache ca-certificates && \
    adduser -D -h /home/releasewave releasewave

COPY releasewave /usr/local/bin/releasewave

USER releasewave
WORKDIR /home/releasewave

ENTRYPOINT ["releasewave"]
CMD ["serve", "--transport=sse"]

EXPOSE 7891
